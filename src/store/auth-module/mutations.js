export function setLogonResponse(state, logonRsponse) {
  state.logonResponse = logonRsponse;
}

export function setUserProfile(state, profile) {
  state.profile = profile;
}

export function setAccessToken(state, accessToken) {
  state.accessToken = accessToken;
}

export function setIdToken(state, idToken) {
  state.idToken = idToken;
}
